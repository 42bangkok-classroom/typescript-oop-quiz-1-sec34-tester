# Quiz 15 - Reverse a Text (loop through characters)
Create a TypeScript program that reads a string from the command line and reverses it by looping through its characters.

## 📄 File to Submit
`Reverse.ts`

## 📘 Instructions
Write a program that:
1. Accepts one argument from the command line (a string).
2. Loops through the characters of the string.
3. Reverses the order of the characters.
4. Prints the reversed string.

## Example:
```
$ ts-node Reverse.ts "hello"
olleh
$ ts-node Reverse.ts "world"
dlrow
$ ts-node Reverse.ts "TypeScript"
tpircSepyT
$ ts-node Reverse.ts "a"
a
```

