# Quiz 17 - Star Pyramid Pattern
Create a TypeScript program that reads a size from the command line and prints a left-aligned ascending star pyramid pattern.

## 📄 File to Submit
`starPyramid.ts`

## 📘 Instructions
Write a program that:
1. Accepts one argument from the command line (a number representing the size).
2. Prints a left-aligned ascending star pyramid where:
   - The first line has 1 star
   - Each subsequent line has one more star than the previous line
   - The pattern continues until reaching the specified size

## Example:
```
$ ts-node starPyramid.ts 5
*
**
***
****
*****
$ ts-node starPyramid.ts 3
*
**
***
$ ts-node starPyramid.ts 1
*
```

